---
export const prerender = true;

import WriteupsView from '../../../views/WriteupsView.astro';
import { langFromParam, prefixedLangPaths } from '../../../i18n/ui';

export function getStaticPaths() {
  return prefixedLangPaths;
}
---

<WriteupsView lang={langFromParam(Astro.params.lang)} />
